export default class DataProcessor {
    /**
     * Takes data and returns it after transforming it
     * @param inputData
     * @returns {Promise<*>}
     */
    process(inputData) {
        return Promise.resolve(inputData);
    }
}
